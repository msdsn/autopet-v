"""Fine-tuning code for the autoPET V interactive model.

Scribble encoding and simulation (guidance, scribble_sim, corrupt), the
batchgeneratorsv2 transform that produces channels 2-4 (interaction_transform), the
trainers, and the one-off setup scripts (init_from_baseline, make_plans,
make_synthetic_dataset, viz).
"""
