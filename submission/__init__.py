"""Grand Challenge submission package (the `/input` -> `/output` entry point).

`python -m submission.process` is the container ENTRYPOINT; the model code itself
lives in `src/` and is imported, not copied.
"""
