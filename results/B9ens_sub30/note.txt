the ensembling is **not recorded in `run.json`** — `args.checkpoint` reads `checkpoint_final.pth` and no ensemble flag appears anywhere in the argument set, so the run's own label is the only evidence of what it did. Treat the configuration as unverified until the run is reproduced with the ensemble recorded in `args`. Unrelated to the 100-case row `B9` (cleanup rule v2 on B3), which shares nothing with it but the letter.
